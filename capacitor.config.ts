import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Infinite_scroll',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
